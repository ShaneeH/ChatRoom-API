FastAPI Chatroom Server

A lightweight real time messaging backend written in Python and powered by FastAPI.
This project includes user validation with Pydantic models, WebSocket echo testing, and basic chatroom endpoints.